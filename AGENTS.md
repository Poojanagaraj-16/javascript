# AGENTS.md

## Repo overview

- Scratch/learning repo for JavaScript experiments.
- No `package.json`, no build tooling, no test framework, no CI.
- Files are standalone `.js` scripts run directly with `node <file>`.

## Conventions

- Node.js is the runtime (implied by `.gitignore`).
- No linting, formatting, or type-checking configured.
